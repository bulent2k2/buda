# Work menu — 2026-08-06

A curated snapshot of what is genuinely open across the opens/wishlist docs,
assembled after PR #594 closed half 1 of opens #8b.  Each entry links to the
document that owns it — **the linked doc is the source of truth**; this menu
is a navigation aid and goes stale the moment items ship.  Items that are
deferred-by-decision or trigger-gated are deliberately NOT re-listed here
(re-opening them is a decision their own docs record the bar for); this menu
covers only (A) items blocked on something outside a development session and
(B) items actionable right now.

## A. Open but NOT actionable from a development session

These four are real opens whose next step belongs to an external resource —
listed first so nobody burns a session rediscovering the blocker.

1. **OA bridge (import/export)** — the last interchange format; the BDB
   persistence substrate it needs is complete (stages 1–5 + GDS round-trip).
   **Blocked on the proprietary Si2 OpenAccess C++ libraries** — nothing can
   start until they are available.  Owner: [opens.md](opens.md) item 7; design
   pattern in [gds_oa_interchange.md](gds_oa_interchange.md); substrate record
   in [wishlist/wishlist-bdb.md](wishlist/wishlist-bdb.md).

2. **Hanan-loci golden regen** — the `hanan_loci` default flip landed
   (branch `claude/hanan-loci-default-flip`) but the 5 content-shifted
   `topo_analysis` goldens must be re-baselined with `--write` on the
   **reference host** (the tool refuses a dirty tree; the regen is expected
   purely additive).  Dependent cleanup once run: `flow/rnr/mix.buda` may
   un-pin from `no_hanan_loci` (its regression was traced to a
   scriptless-harness artifact and is resolved).  Owner:
   [wishlist/wishlist-topo.md](wishlist/wishlist-topo.md) "Nominal-WL comparability" /
   [opens.md](opens.md) item 13 residual; procedure in
   [hanan_loci_golden_regen.md](hanan_loci_golden_regen.md).

3. **bigHalf rr slow-tier endpoint under the loci default** —
   `test_bighalf_rr_reaches_clean_endpoint` (slow tier) misses the clean 0/0
   endpoint on the **reference host** under the default-on loci pool; owner
   decision at flip time was "ship and debug later".  First debugging step
   (stated): the same occupancy/trial trace used for the mix–loci
   interaction, run against the loci pool — on the host that shows it.
   Owner: [wishlist/wishlist-topo.md](wishlist/wishlist-topo.md) "Follow-on — bigHalf rr
   slow-tier endpoint under the loci default".

4. **The Scala.js LINK step** — the web-port execution gap is otherwise
   closed (JS via node, Scala via JVM shim), but the *linked* Scala.js
   artifact is untested, so a Scala.js-semantics divergence (e.g. uniform
   `Double` numerics) is invisible.  **Blocked on sbt + the scalajs plugin**
   — hand-assembled `scalac -scalajs` classpaths die in the compiler
   (recorded attempts in the doc).  Owner: [opens_ci.md](opens_ci.md) item 4,
   "The Scala.js link step — attempted, blocked, recorded".

## B. Actionable now

Ordered roughly by recommendation, not size.

0. **NDR support (non-default width / spacing / shield constraints)** —
   NEW feature open (2026-08-06): let nets/buses/bundles carry named rules
   for wider wires, extra clearance, and shield flanking.  Requirements are
   written — grounded in the one-bit-one-SIGNAL-slot assumption the whole
   pipeline makes today and the single-sourced track-demand arithmetic it
   must keep — and four candidate solution paths are sketched for a design
   brainstorm (slot-quantized consumption / pattern-declared classes /
   continuous packing / hybrid).  Next step is confirming the recorded
   path leaning + UI strawman, not coding.  Owners (audience-split):
   [../NDR_REQUIREMENTS.md](../NDR_REQUIREMENTS.md) (architects),
   [../NDR_UI.md](../NDR_UI.md) (methodology),
   [ndr_architecture.md](ndr_architecture.md) (implementers);
   [opens.md](opens.md) item 15.

1. **Alignment-aware `align_bottom_up`** — let the aligner see top-level
   track demand when choosing a phase, so the b61-class contention geometry
   never forms: every convertible doomed seat found in the 2026-08 corpus
   sweeps sits in a phase-snapped bottom-up vehicle, and
   `mix2_fast_on_aligned_sql` sticks at 2 ov / 16 opens while its unaligned
   twin lands 0/0/0.  Explicitly recommended by its doc as the step to take
   *instead of* class-level track negotiation (whose trigger UN-fired
   2026-08-04).  Owner: [wishlist/wishlist-healer.md](wishlist/wishlist-healer.md)
   "Class-level TRACK negotiation" → the two cheaper angles.

2. **Tug-of-war consumer + kWLSpread path (b)** — one arc, two docs.  The
   opposite-pull pair *detector* shipped (b44's +1133 trunk stretch; 7 pairs
   bigHalf, 6 mempool_tile, 3 mix) but nothing consumes it; separately the
   `kWLSpread` default flip is refused because the whole-envelope term
   regresses mix2 42→52 even with healers, and the one named route to a safe
   default is a spread term pricing only the **trunk-stretch
   (junction-coupled)** component — which is exactly what the tug-of-war
   pairs identify structurally.  Owners:
   [wishlist/wishlist-nuts.md](wishlist/wishlist-nuts.md) "Opposite-pull ('tug-of-war')
   connector pairs" and [wishlist/wishlist-planner.md](wishlist/wishlist-planner.md)
   "Realization-risk WL" (default-flip verdict, path (b)).

3. **The untaken re-seat measurement on a healing flow** — chip3a's
   `FREE_SIBLING` re-seat is proposed by the heal and correctly rejected
   (+14 unplaced / +3 ov), but the chip vehicles are healerless so the delta
   never gets a chance to be recovered; its doc calls this "the one
   measurement that could still change the picture".  Mostly flow assembly +
   corpus runs.  Owner: [wishlist/wishlist-healer.md](wishlist/wishlist-healer.md)
   "Re-measured 2026-08-04", final paragraph.

4. **Occupancy-aware supply at plan time** — the heavy one behind bigHalf's
   residual 94 opens (48 LOW supply-short + 46 occupancy-short); known trap:
   extending dead-span escalation from `pool == 0` to `pool < nbits`
   regresses 94→270 because the supply model sees keepouts but not
   occupancy.  Also the third gate on the `charge_pull_target` default flip.
   Multi-PR arc.  Owner: [wishlist/wishlist-planner.md](wishlist/wishlist-planner.md)
   "NON-TOP dead-span stub opens", closing section.

5. **Quick wins** (afternoon-sized):
   - **TEG-over `stub_suppressed` pre-pass fix** — the trunk generator's
     gap-block pre-pass skips suppressed blocks while emission still emits
     their gap stubs, so the islands gate drops the candidate: a pure
     pool-completeness loss.  Owner: [wishlist/wishlist-topo.md](wishlist/wishlist-topo.md)
     "Audit 2026-07: stub_suppressed TEG-over blocks skip the spine
     pre-extension".
   - **`add_topology_segment` prepared-statement cache** — the last item of
     the persist-batching series (~0.07 s / 5990 calls on slowdown.buda;
     mirror the hot read paths' cached statements).  Owner:
     [wishlist/wishlist-bdb.md](wishlist/wishlist-bdb.md) "Remaining smaller target".

6. **Investigation-flavored, low urgency** (each explicitly so in its doc):
   - `set_drop_dangling` four open questions — incl. whether the ±2³⁰
     unbounded slide window is ever load-bearing and whether `clamp` can be
     made provably inert.  Owner: [wishlist/wishlist-topo.md](wishlist/wishlist-topo.md)
     "Dangling-topo handling — further investigation".
   - Single-layer rerun placement drift (`run_nuts_on_layer` one-time trunk
     move + one extra overlap vs the full solve).  Owner:
     [wishlist/wishlist-nuts.md](wishlist/wishlist-nuts.md) "Audit 2026-07: single-layer rerun
     placement drift".
   - Over-capacity classification before the repair loop — tag
     supply-exceeded bands up front so corner/repair stop burning ~50 s on
     unfixable overlaps and report them as the planner's ALLOW_OVERFLOW
     problem.  Owner: [wishlist/wishlist-nuts.md](wishlist/wishlist-nuts.md) "Corner/repair at
     scale" item (3).
   - Envelope term pricing edge-vs-interior optima — a floor-locus candidate
     whose optimum sits at the window edge realizes ~2.6% worse than the
     interior sibling while scoring equal (b44 x1200 vs x1950).  Owner:
     [wishlist/wishlist-topo.md](wishlist/wishlist-topo.md) "Nominal-WL comparability"
     interplay caution.

## Not re-listed here

Deferred-by-decision and trigger-gated items stay in their owning docs with
their revival bars: class-level TRACK negotiation, the NUTS-side alignment
pre-solve (opens #8, Big/blocked), pair-align half 2 (opens #8b) and the
refused pair-align default flip, the non-TOP span-stretch clamp, warm-trials
default, `spine_relays`/`multi_trunk`/`set_dedup_loci`/`kPeak` default
flips, supply-driven bundle caps ([wishlist/wishlist-bundler.md](wishlist/wishlist-bundler.md)),
slide-aware pass-through, incremental re-analysis, and the smaller BDB
deferrals ([wishlist/wishlist-bdb.md](wishlist/wishlist-bdb.md)).  Consult
[opens.md](opens.md) and the wishlists before reviving any of them.
