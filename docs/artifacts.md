Artifacts
==

Published pages that came out of BUDA sessions.  Some are obsolete by now,
but there is historical value in them, and several have a twin kept in the
tree that outlives the link.

**Dates.**  *v1* is the earliest date the page is known to have existed: the
commit that first recorded its link in this repository where there is one,
otherwise the day it was first entered here.  *latest* is the artifact
service's last-updated date, read on 2026-09-13; a row revised since carries
the date of that revision instead (the investor briefing's 2026-09-24).  A
single date means the page was published once and never revised.

| artifact | what it is | v1 | latest | in the tree |
|---|---|---|---|---|
| [16-Core SoC Compaction](https://claude.ai/artifact/ShDEKKng5AWZj5qkiLk5Cm) | How small the SoC vehicle's floorplan (`soc.tcl 8 -LAYOUT compact`) can get before its wiring stops fitting, in two parts: a plain-language overview for readers outside chip design (blocks, buses, the three space knobs in a diagram, a chart of every configuration routed, what was tried and the four lessons) and the detailed results of four rounds — padding and channel (the grid at PAD 10 GAP 4, 0.68x the die, clean in ~13 s), a slicing packer and leaf aspect (the whitespace is the routing), each cell routed alone (`regf` under-sized by the per-pin face rule; local cost does not predict the chip), and `-FACES` with a chip-scored plan search (the first check does not predict the heal; no plan beat the grid) | 2026-09-25 | 2026-09-26 (v5: round 3's phase run re-measured with `-at chip` shifting the tracks rather than the seat, the die the same size in both modes: 185 bits against 173, and the conclusion unchanged. v4: every failing end result also counts its dirty bundles -- `168u, 1o, 8b`, the final `check_design`'s flagged bundles joined with both bundles of each overlap read from the saved route -- in the tables, the hover and the viewer, and every dot carries the one-line command that reproduces it. v3: every dot on the chart opens its floorplan and routed bit-wires, each re-run from the committed tree and drawn from the BDB it saved — 45 of the 47 reproduce their recorded die and end state, the two aspect points came from an uncommitted edit and say so; and round 3's track-phase sentence corrected: `-at chip` had kept the phase in x only, and on the chip's phase in both axes the local run still strands `id`, `x` and `l1dd` and not `m` and `dd`, 184 bits against 173, so the conclusion stands and the bit-for-bit reading is withdrawn. v2: round 4's re-samples corrected — the plan search matched plans across perturbations by pairwise child order, which maps 204 plans onto 156 keys, so the 'blow-ups' of plans 112, 164 and 186 were other plans' runs; re-run with each plan matched by its slicing tree, all three hold, plan 112 joins the healed set and ends 480 bits short, and the findings stand) | [soc.md](../flow/tcl/soc.md) ("Compaction at NQ = 8" through "Round 4") |
| [BUDA: Progress and the Road Ahead](https://claude.ai/code/artifact/d824d2d5-8b8c-407e-bf64-7aa99aa45fc9) | Briefing for prospective angel investors, written for readers outside chip design (a plain-language summary up front, floorplan and wiring pictures of two test designs and the five hardened arrays of the compaction study at one scale, the results table explained, a glossary at the end): what BUDA is, its Intel Galaxy origin, the 2026 timeline, capabilities and the numbers behind them, the LibreLane N = 8 result, and a future-challenges section (technical, product, deliberate limits) plus the founder's to-do list of what the repository cannot supply | 2026-09-20 | 2026-09-24 | [investor_briefing_2026-09.md](internal/investor_briefing_2026-09.md) |
| [BUDA Convergence Ladder](https://claude.ai/code/artifact/834de29e-3848-487a-b864-eda0a88c8cea) | Experiment plan for showing hierarchical convergence with BUDA in the loop — the information ladder, five experiments, rendered soc/tpu floorplans with NUTS + DNUTS, three decisions, and the first two experiments measured (E4 diagnosed-vs-blind, E2 blob/bbox/exact with its NQ = 8 panels), build items 3, 4, 5 and 6 landed (`report_layer_demand` / `buda::query demand`, `derive_cell_layer_shares`, `flow/tcl/converge.tcl`, `set_cell_layer_reserve` + `derive_cell_layer_reserves`), E1 run — the claim refuted for the share primitive, with the reason measured — and E5 run against the positional primitive, item 6c — the top's plan handed down — built and measured on E5's loop, and item 6d — the derivation yielding the block its seat — built and measured on the same loop | 2026-09-13 | 2026-09-18 (v17: the healed NQ = 16 top-down arm FINISHED and corrects v14's reading — it does reach 0/0/0, at round 2, so SEVEN of the eight healed arms are clean and v14's 'six' was a verdict taken off round 1 while round 2 was still solving; what the re-measurement moved is what that arm COSTS to clean rather than whether it does, its second round entering at 46/1032/1032 and healing in 9526 s against round 1's 220 s.  v16: a SCOPE note, mirrored in the tree twin — the objective is CONVERGENCE, not minimum area (area and wire are the PRICE of a policy, and one that wins area while costing a re-spin has lost), and the techniques are stated in terms of the block/context BOUNDARY rather than metal, so they apply to any hierarchical structure; the two vehicles are instruments, not the subject.  v15: the SoC's TWO FLOORPLANS compared — `-LAYOUT band` against `-LAYOUT compact` at NQ = 32, both rendered and both clean top-down: compact is a 4x8 grid with no holes at 0.914 utilization against band's 6x6 with four empty slots at 0.784, a die 14% smaller whose aspect flips landscape to portrait, for +1.5% abstract and detailed wire on the same 1235 bundles; band stays the default because the vehicle's recorded sweep has compact dirtier at every size BOTTOM-UP, which is the arm the ladder runs on.  v14: the 6d tables RE-MEASURED again after a second review round — the yield's contiguous-run test was reading a single-x probe while the need it was judged against came from the seat's span-clear admission pool, so a keepout along the span could make it call a seat fine that DetailedNUTS then strands.  All three tables are now from one engine: the mesh control reproduces exactly, the healerless table moved nine cells (seven in `yielded`), and the healed table moved ONE row — NQ = 16 top-down from 0/0/0 to 2/0/0 — so SIX of the eight healed arms are clean rather than seven, both exceptions top-down.  v13: 6d RE-MEASURED after a review found the yield judging each seat against half of what fragments it — the cell's own line and the corridors it inherits are now one blocked set, over every occurrence, read from the map that will be in force: the NQ = 2 top-down round strands nothing where 6c stranded 360, at the price of one overlap no healer clears, seven of eight healed arms clean, 8 bits gained at NQ = 16 top-down and 8 lost at NQ = 2 bottom-up; the first measurement's headline cost (264 stranded at NQ = 16 bottom-up) was the defect, not the policy, which measures 0 overlaps and the baseline's 40 bits; v12: item 6d built and measured — `derive_cell_layer_reserves yield`, `converge.tcl -yield`: the block keeps its seat where the corridor leaves it no contiguous run, nested inherited corridors included; the NQ = 2 top-down round clean healerless in one round, every healed arm clean at every size, the loss handed to the pinned top where the two collide (NQ = 16 bottom-up 40 → 264) — a lever, not a default; those figures are SUPERSEDED by v13; v11: item 6c built and measured — `derive_top_plan` → `pin_plan`, `converge.tcl -handdown`: the loop has a fixpoint at every size on both vehicles (exact, 1.00×, on the mesh) and, handed a healed blind round, the informed round is clean healerless in one round at NQ ≤ 8; the fixpoint is as clean as the top handed down; v10: item 6b built and measured — `set_reserve_steer`, off by default: the mesh's hit rate 0.00 → 1.00 at no cost, the SoC's informed rounds dirtier at NQ ≥ 4; E5's 6–11 % hit rate corrected as a mis-measurement (0.65–0.97 read right); item 6c, the top's plan handed down, next; v9: E5 run and written up — the guessed reservation worse than none, the derived one clean at three of four sizes healerless, item 6b the top-side half; v8: E1's blind reservation re-priced over the capped cells only, `reserve_top_layers` leaving the top level unrestricted — Codex on #935; v7: E1 run and written up; item 5 — the loop driver — built; the corridor primitive moved ahead of the fixed-pin work) | [convergence_ladder.md](internal/convergence_ladder.md), [convergence_e4.md](internal/convergence_e4.md), [convergence_e2.md](internal/convergence_e2.md), [convergence_e1.md](internal/convergence_e1.md); pictures regenerated by `tools/render_design.py` and `tools/experiment/e2_abstract_precision.py` |
| [Fixed Pin Primitive](https://claude.ai/artifact/T75FBqiYjTEax8A5Q5jdLN) | Design proposal for `fix_pin`, convergence-ladder build item 7 — one command from "horizontal only" to "this bit at this point on this layer", built as a rewrite of the bundle's busterm into admissible face strips (the representation a DEF die port already has), with the layer and bit-order knobs as side channels; seven pushbacks and eight open questions published for review by people with real pin-assignment flows. Not built. | 2026-09-20 | 2026-09-20 (v2: four review findings folded in — a strip carries a FACE TAG, since the landing annotator accepts any edge of a rect; the admissible geometry is single-sourced to the pass-through clamp and the audit, which read the floorplan and not the busterm; a pin's METAL layer is distinct from the APPROACH layer, with an endpoint via per bit; bit order is per LANDING, refused when two landings on one straight segment disagree; v3: the second round folded in — TWO geometries, landing and obstruction, since one resolver would have made a fixed macro's body routable on LOW layers; the physical-face spelling replaced under a fix; a declared window not margin-inset; the die-port test restated as a relation; the auto bit cap reads the window; strips contribute along-axis loci only; Q2 decided as a split; Q7 named the gate; PR 1 split into a byte-identical refactor and the command; the contract in `test/tests/features/fixed_pin.feature`; v4: the third round, Codex — the endpoint via as a via KIND of its own rather than the bond strap's negative row, which three consumers read as a strap; the approach layer as a per-segment SET on the landing stub, since the per-bundle mask starves an L's other leg and the per-segment pin holds one layer; a pass-through as TWO landings, so a single-face or point fix admits none whatever the strip's width; order agreement checked per candidate at generation, since a fix precedes any segment; v5: the fourth round, the owner — the landing stub's layer set OUTSIDE the candidate fingerprint (hashing it would churn every persisted `topo_uid`, the `perp_clamp` precedent) and INSIDE the schema as a column; the via sign's fourth reader, the R9 audit index, which would read an endpoint via as proof of bonding; `set_bus_layers` named as level 4 at bundle scope with the fix's set the fourth intersecting claimant, judged after `_layer_masks_resolved`; the target's pin part a longest-match prefix, not a glob; the two pass-through reporters in the census; the rounds log's one contradiction marked superseded; v6: the fifth round, the owner, verdict MERGEABLE — a per-instance fix on a `set_bottom_up` class refused like R2d rather than asserted consistent, since `inst:` beats `cell:` and a copied class cannot carry one occurrence's fix; `from_pins` a BDB projection inside the schema bump (the LEF reader already keeps every port's layer and rects) and a pin PORTS, plural; the fix-uniform split placed FIRST in the bundlers' order since the auto cap reads a key the split defines; `PIN_FIX`'s third, hand-kept site named with its guard; the one surviving `BUDA-192x` made BUDA-1922) | [fixed_pin_design.md](internal/fixed_pin_design.md) |
| [LibreLane Stage Sheet](https://claude.ai/code/artifact/e2e7e6b0-b4c2-434c-b99a-dc5b2215ba47) | Stage-by-stage sheet of the LibreLane RTL-to-GDS flow as BUDA meets it | first entered here 2026-09-13 | 2026-09-09 | [librelane_hier_flow.md](internal/librelane_hier_flow.md) |
| [BUDA × LibreLane Hierarchical Flow](https://claude.ai/code/artifact/b58fd737-bda2-4c30-bffa-d11fd1832592) | The plan for BUDA inside a LibreLane hierarchical flow, with its runnable phase 0 | first entered here 2026-09-13 (the doc is dated 2026-09-05) | 2026-09-07 | [librelane_hier_flow.md](internal/librelane_hier_flow.md) |
| [BDB Schema v29](https://claude.ai/code/artifact/0c67463d-2e6d-44e8-a151-ec692b2acb18) | The BDB schema at v29, table by table | 2026-08-19 | 2026-08-19 | [BDB_REFERENCE.md](BDB_REFERENCE.md) |
| [Heal-to-Clean Traces](https://claude.ai/code/artifact/01334610-e7f5-4b58-80d8-21e97d386ba5) | Healer trajectories on the rnr vehicles, overlap/opens per iteration | 2026-08-12 | 2026-08-18 | [healer_effectiveness_2026-07.md](internal/healer_effectiveness_2026-07.md) |
| [Layer Caps & Shares — Design Plan](https://claude.ai/code/artifact/b714b9e6-d49f-42c2-bc33-2b7da591c095) | Layer resource sharing across the hierarchy: bands, shares, by-depth and stack-relative declaration | 2026-07-31 | 2026-07-31 | [hier_layer_caps.md](internal/hier_layer_caps.md) |
| [Layer Caps & Shares — Design Plan (earlier copy)](https://claude.ai/code/artifact/a5359e6e-bbb7-4ee2-8465-0815e039d8ed) | A second publish of the same plan, same day; the one above is the one the repo cites | 2026-07-31 | 2026-07-31 | — |
| [Pure-MST Segment-Count Study — bigHalf](https://claude.ai/code/artifact/aa189092-4d9f-4152-928f-6f2c218f3e05) | Pure-MST segments aren't bounded by shape — they're bounded by star hubs | 2026-07-28 | 2026-07-27 | [mst_edge_realization.md](internal/mst_edge_realization.md) |
| [BUDA · Subsystem Interactions](https://claude.ai/code/artifact/087b0257-4001-4a97-b460-1d08efcb3794) | The big picture: topo-gen ↔ planner ↔ healer interactions, with the live-trace charts | 2026-07-22 | 2026-07-22 | [big-picture-2026-07-22.md](internal/big-picture-2026-07-22.md) |

Where *v1* is later than *latest* (the MST study) the link was recorded in the
repo the day after its last revision; *v1* there is an upper bound, not the
publish date.
