def_layer 4 M4 V TOP 10
def_layer 5 M5 H TOP 10
def_layer 6 M6 V TOP 10

add_block b1 0 0 100 100
add_block b2 200 200 300 300
add_bus v[4] b1.o b2.i
add_bus w[4] b2.i b1.o
run_bundler strict
generate_topologies
visualize_topologies
# both bundles pick the third topo
# select_topologies 1,2 3
# first bundle picks the third topo, second bundle picks the fourth topo
select_topologies 1 3 2 4
# all bundles are assigned a topology, so no need to run the bundler
run_nuts
visualize
