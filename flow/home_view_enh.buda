source tracks/tracks.buda
add_block u1 0   0 100 100
add_block u2 50 50 150 150
add_bus b[8] u1 u2
run_bundler
detour_channel A 100
generate_topologies double_detour
select_topology 1 5
run_planner
run_nuts
run_detailed_nuts
visualize
# now do `v` and pin the first topology
# then do `v` again to go back to home
# hit `h`, but it still stays in the original view
