New: 

1) Testcase: two_busterms_rotated.buda. Bundle 3. Topo 4. What's the sliding range for the H seg? It should be the whole right edge.
2) Testcase: two.buda. Bundle 4. Topo 1 (I_H): slide-in-y=[500, 580]. Shouldn't it be [460, 580]? Similarly for Topo 3, H-seg connecting to u14. 
3) Same test case, same bundle. Why does `run_planner` pick that topo? There are shorter topos?

Last prompt:
❯ Doesn't it make sense to apply corner margins on the busterm.rect and then use those rects to create the hanan grid? Then, topo generation code get simpler by using busterm.rects as is.

"two.buda" topologies for Bundle 13 have a few issues: 1) 

test case "two.buda" [Planner] Bundle 13 (1.5 units wide) -> L_HV  [H→M4 V→M5]  overflow=0. slide ranges are too tiny for both segs. Second issue: for topo 2 (L_VH) the segs are placed outside the slide ranges. 

bundle 1, topo 1 (L_HV): the ranges are good, but each seg is placed outside its range. They are pulled all the way to the corner. They should stay within their slide-ranges. Topo 2 (L_VH) has a good V seg, but the H seg has the same problem as in topo 1. Finally, topo 5 (U_HVH): The V segment can have a wider slide-range. It should not be limited to one hanan grid, because it is detouring outside of the bbox of the busterms anyway.

two_busterms.buda, bundle 4: some topos (e.g., topos 1, 2, 3 and more) use wrong busterm label (u13) instead of the correct one (u14). 

two_busterms, bundle 3: Same bundle is missing another L-topo. See the busterms partially overlap in y. So, we have two options for the H trunk: 1) under the busterm on the right. 2) above the busterm on the left. We have only the first. Second one is missing. As I said earlier, we need to consider all H hanan grids as a potential H trunk location and see if a V stub makes sense. If the two busterms partially overlap in  x, we have the symmetric case. 

DONE. Test:
Testcase two_busterms.buda has variations of the same L-topo problem: 1) topo 3 of bundle 3, 2) topos 2 and 3 of bundle 4, 3) topos 3 and 4 of bundle 5
